# Local Supabase Auth OAuth credentials.
# Copy this file to supabase/.env. The CLI substitutes env() values in config.toml.
# These are Auth service secrets, not Next.js env vars. Never commit supabase/.env.

# Google Cloud OAuth Web client (Authorized redirect URI:
# http://127.0.0.1:55321/auth/v1/callback)
SUPABASE_AUTH_EXTERNAL_GOOGLE_CLIENT_ID=
SUPABASE_AUTH_EXTERNAL_GOOGLE_SECRET=

# GitHub OAuth App (Authorization callback URL:
# http://127.0.0.1:55321/auth/v1/callback)
SUPABASE_AUTH_EXTERNAL_GITHUB_CLIENT_ID=
SUPABASE_AUTH_EXTERNAL_GITHUB_SECRET=
